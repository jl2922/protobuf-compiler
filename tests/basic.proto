enum Type {
    EMAIL = 1;
    TEXT = 2;
}

/* Message Description. */
message Message {
    /* More description. */
    required string title = 1; // Field description.
    optional string content = 2;
    repeated Author author = 3;
    optional Type type = 4;
}